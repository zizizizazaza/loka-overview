import React from 'react';
import ReactDOM from 'react-dom/client';
import LandingLoka from './LandingLoka';

ReactDOM.createRoot(document.getElementById('root') as HTMLElement).render(
  <React.StrictMode>
    <LandingLoka />
  </React.StrictMode>,
);
